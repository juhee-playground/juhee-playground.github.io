export enum PortfolioState {
  START = 'START',
  MENU = 'MENU',
  RESUME = 'RESUME',
  DASHBOARD = 'DASHBOARD',
  PORTFOLIO = 'PORTFOLIO'
}

export type PowerState = 'on' | 'off' | 'powering-on' | 'powering-off';